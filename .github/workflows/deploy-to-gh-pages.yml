name: Deploy to GitHub Pages
on:
    workflow_dispatch:

defaults:
  run:
    working-directory: app
    

jobs:
    deploy-gh-pages:
        runs-on: ubuntu-latest
        steps:
            - run: echo $(ls)


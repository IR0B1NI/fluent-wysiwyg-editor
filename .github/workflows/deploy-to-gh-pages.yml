name: Deploy to GitHub Pages
on:
    pull_request:
        branches:
            - main
        types: [closed]

defaults:
  run:
    working-directory: ./app
    

jobs:
    deploy-gh-pages:
        runs-on: ubuntu-latest
        steps:
            - run: |
                yarn
                yarn run deploy

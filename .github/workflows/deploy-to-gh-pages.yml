name: Deploy to GitHub Pages
on:
    workflow_dispatch:    

jobs:
    deploy-gh-pages:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: |
                cd app
                yarn
                yarn run deploy
